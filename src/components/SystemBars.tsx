import { SystemBars as EdgeSystemBars } from 'react-native-edge-to-edge';

import type { SystemBarsProps } from 'react-native-edge-to-edge';

const SystemBars = (props: SystemBarsProps) => {
  return <EdgeSystemBars {...props} />;
};

export default SystemBars;
